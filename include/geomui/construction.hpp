#include "solver.hpp"

#include <functional>

namespace geomui {

// shapes just return a set of vars and constraints
// typedef std::pair<Vars, Problem> ConstrShape;

// ConstrShape HorizontalLine(std::string prefix, std::string name="horiz", Var x1, Var x2, Var y) {
//     const auto y1 = std::make_shared<Var>(prefix + 
//     const auto y2 = std::make_shared<Var>(prefix + "/y2", 0);
// }

// ConstructionShape 

} // namespace geomui